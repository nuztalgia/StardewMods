namespace Nuztalgia.StardewMods.DSVCore;

internal static class GlobalOptions {

  internal enum StandardVariant {
    Vanilla,
    Modded,
    Off
  }

  internal enum MinimalVariant {
    Modded,
    Off
  }

  internal enum StandardLightweightConfig {
    Full,
    Light,
    Ultralight
  }

  internal enum MinimalLightweightConfig {
    Full,
    Light
  }

  internal enum Pyjamas {
    True,
    False,
    Marriage
  }

  internal enum Tattoos {
    Tattoos,
    NoTattoos
  }
}
